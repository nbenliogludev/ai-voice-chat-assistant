# Frontend

React interface for the AI voice chat assistant.

## Scripts

```bash
npm install
npm run dev
npm run build
```

By default, development requests to `/api` are proxied to `http://localhost:3000`.
Set `VITE_API_URL` if the backend runs on another host.
